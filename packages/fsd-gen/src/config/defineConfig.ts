import type { FsdGenConfig } from './types.js';

/**
 * Define configuration for fsd-gen.
 * @param config User configuration
 * @returns User configuration
 */
export function defineConfig(config: FsdGenConfig): FsdGenConfig {
    return config;
}
